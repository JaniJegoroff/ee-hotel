EE Hotel Booking UI tests 
==========

EE hotel booking basic acceptance UI tests.

Default `raketask` will also execute `Rubocop` static code analysis.

Prerequisites
==========

`$ bundle install`

Run the tests
==========

`$ bundle exec cucumber`

or 

`$ bundle exec rake`

License
==========

MIT
